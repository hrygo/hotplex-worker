gateway:
  addr: ":8888"
  broadcast_queue_size: 256

admin:
  enabled: true
  addr: ":9999"

security:
  tls_enabled: false

session:
  retention_period: "168h"
  gc_scan_interval: "5m"

pool:
  max_size: 100
  max_idle_per_user: 3

db:
  path: "data/test.db"
  wal_mode: true
